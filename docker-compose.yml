version: '3.8'
services:
  postgres:
    image: postgres:15
    env_file:
      - ./.env
    ports:
      - '5432:5432'

  api:
    env_file:
      - ./.env
    build:
      dockerfile: Dockerfile
    links:
      - postgres
    ports:
      - '3000:3000'
